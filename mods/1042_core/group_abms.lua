--[[core.register_abm({
    interval = 4,
    chance = 1,
    nodenames = {"group:melts"},
    action = function(pos, node, active_object_count, active_object_count_wider)
        -- Add melt code
    end
})]]